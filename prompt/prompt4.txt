为了使得多个诱导灯设备实现同步闪烁，本系统仿照 NTP 协议实现 LoRa 时间同步，确保终端设备与真实时间一致。时间同步过程如下：

终端设备向服务器发送时间同步请求，携带本地发送时间 T1。
服务器接收请求，记录接收时间 T2，并发送响应，携带发送时间 T3。
终端设备接收响应，记录接收时间 T4。
终端设备计算时间偏移量：offset = ((T2 - T1) + (T3 - T4)) / 2，并调整本地时间。
最后基于调整后的本地时间，调整LED的闪烁时刻。